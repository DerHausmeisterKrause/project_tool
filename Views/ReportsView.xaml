<UserControl x:Class="TaskTool.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgBrush}">
    <Grid Background="{StaticResource BgBrush}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <ScrollViewer VerticalScrollBarVisibility="Auto" Background="Transparent">
            <StackPanel>
                <UniformGrid Columns="2" Margin="0,0,0,12">
                    <Border Style="{StaticResource CardStyle}"><StackPanel><TextBlock Text="Ticket-/Aufgabenzeit heute" Foreground="{StaticResource MutedTextBrush}"/><TextBlock Text="{Binding TicketMinutesToday, StringFormat={}{0:N0} Min.}" FontSize="26" FontWeight="Bold"/></StackPanel></Border>
                    <Border Style="{StaticResource CardStyle}"><StackPanel><TextBlock Text="Ticket-/Aufgabenzeit Monat" Foreground="{StaticResource MutedTextBrush}"/><TextBlock Text="{Binding TicketMinutesCurrentMonth, StringFormat={}{0:N0} Min.}" FontSize="26" FontWeight="Bold"/></StackPanel></Border>
                </UniformGrid>

                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <DockPanel Margin="0,0,0,8">
                            <TextBlock Text="Top Tasks diesen Monat" FontSize="16" FontWeight="SemiBold"/>
                            <Button Content="Aktualisieren" Width="110" HorizontalAlignment="Right" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding RefreshCommand}"/>
                        </DockPanel>
                        <ItemsControl ItemsSource="{Binding TopTasks}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,8"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="120"/></Grid.ColumnDefinitions><TextBlock Grid.Column="0" Text="{Binding Title}" TextTrimming="CharacterEllipsis"/><TextBlock Grid.Column="1" Text="{Binding DurationText}" HorizontalAlignment="Right" Foreground="{StaticResource MutedTextBrush}"/></Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
