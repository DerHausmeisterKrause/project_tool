<UserControl x:Class="TaskTool.Views.AiChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:TaskTool.Models">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="KI" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,6"/>
        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="{Binding ProviderDescription, StringFormat=Aktiver Provider: {0}}" Foreground="{StaticResource MutedTextBrush}"/>
            <TextBlock Text="{Binding AvailabilityMessage}" Foreground="#FBBF24" TextWrapping="Wrap" Margin="0,5,0,0"/>
            <Button Content="KI-Einstellungen öffnen" Command="{Binding OpenAiSettingsCommand}" HorizontalAlignment="Left" Margin="0,8,0,0">
                <Button.Style><Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding IsDisabled}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></Button.Style>
            </Button>
        </StackPanel>

        <Border Grid.Row="2" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrushColor}" BorderThickness="1" CornerRadius="12" Padding="12">
            <ListBox x:Name="MessageList" ItemsSource="{Binding Messages}" Background="Transparent" BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListBox.ItemContainerStyle><Style TargetType="ListBoxItem"><Setter Property="HorizontalContentAlignment" Value="Stretch"/><Setter Property="IsHitTestVisible" Value="False"/><Setter Property="Margin" Value="0,4"/><Setter Property="Background" Value="Transparent"/></Style></ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:AiChatMessage}">
                        <Border x:Name="Bubble" Background="#172554" CornerRadius="10" Padding="12" MaxWidth="720" HorizontalAlignment="Left">
                            <StackPanel><TextBlock Text="{Binding Author}" FontWeight="SemiBold" Foreground="#93C5FD" Margin="0,0,0,5"/><TextBlock Text="{Binding Content}" TextWrapping="Wrap"/></StackPanel>
                        </Border>
                        <DataTemplate.Triggers><DataTrigger Binding="{Binding IsUser}" Value="True"><Setter TargetName="Bubble" Property="HorizontalAlignment" Value="Right"/><Setter TargetName="Bubble" Property="Background" Value="#1F2937"/></DataTrigger></DataTemplate.Triggers>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <TextBox Grid.Row="3" Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="95" MaxHeight="180"
                 VerticalScrollBarVisibility="Auto" Padding="12" Margin="0,12,0,8" KeyDown="InputTextBox_OnKeyDown" IsEnabled="{Binding IsEnabled}" ToolTip="Nachricht eingeben …"/>
        <Grid Grid.Row="4"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel><TextBlock Text="{Binding SendingStatus}" Foreground="{StaticResource MutedTextBrush}"/><TextBlock Text="{Binding ErrorMessage}" Foreground="#F87171" TextWrapping="Wrap"/></StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal"><Button Content="Chat leeren" Command="{Binding ClearCommand}" Style="{StaticResource SecondaryButtonStyle}"/><Button Content="Senden" Command="{Binding SendCommand}"/></StackPanel>
        </Grid>
    </Grid>
</UserControl>
