<Window x:Class="TaskTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:TaskTool.Views"
        Title="TaskTool" Height="820" Width="1480" MinHeight="760" MinWidth="1280"
        Background="{StaticResource BgBrush}">
    <Window.Resources>
        <Style x:Key="SidebarNavigationItemStyle" TargetType="ListBoxItem">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="3,0,0,0"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="NavigationBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="9" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="NavigationBorder" Property="Background" Value="#172033"/></Trigger>
                            <Trigger Property="IsSelected" Value="True"><Setter TargetName="NavigationBorder" Property="Background" Value="#172554"/><Setter TargetName="NavigationBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}"/></Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True"><Setter TargetName="NavigationBorder" Property="BorderBrush" Value="#60A5FA"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Background="{StaticResource BgBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="170" />
            <ColumnDefinition>
                <ColumnDefinition.Style><Style TargetType="ColumnDefinition"><Setter Property="Width" Value="*"/><Style.Triggers><DataTrigger Binding="{Binding IsTodaySelected}" Value="True"><Setter Property="Width" Value="3*"/></DataTrigger></Style.Triggers></Style></ColumnDefinition.Style>
            </ColumnDefinition>
            <ColumnDefinition>
                <ColumnDefinition.Style><Style TargetType="ColumnDefinition"><Setter Property="Width" Value="0"/><Style.Triggers><DataTrigger Binding="{Binding IsTodaySelected}" Value="True"><Setter Property="Width" Value="2*"/></DataTrigger></Style.Triggers></Style></ColumnDefinition.Style>
            </ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrushColor}" BorderThickness="0,0,1,0">
            <StackPanel Margin="10">
                <TextBlock Text="TaskTool" FontSize="22" FontWeight="SemiBold" Margin="8,6,8,16" />
                <ListBox ItemsSource="{Binding NavigationItems}" SelectedItem="{Binding SelectedView}" Background="Transparent" BorderThickness="0"
                         ItemContainerStyle="{StaticResource SidebarNavigationItemStyle}" KeyboardNavigation.TabNavigation="Continue">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <Button Command="{Binding NavigateToSettingsCommand}"
                        CommandParameter="Updates"
                        Visibility="{Binding SettingsViewModel.IsUpdateAvailable, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Foreground="#EF4444" FontWeight="SemiBold" FontSize="12"
                        Background="Transparent" BorderThickness="0" Padding="8,4"
                        Margin="8,4,8,0" HorizontalContentAlignment="Left"
                        Cursor="Hand" ToolTip="Update in den Einstellungen anzeigen">
                    <TextBlock Text="{Binding SettingsViewModel.UpdateBannerText}" TextWrapping="Wrap"/>
                </Button>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Padding="16" Background="{StaticResource BgBrush}">
            <Grid>
            <ContentControl Content="{Binding SelectedView}"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch" />
            <Grid x:Name="PersistentShortcutBrowserHost" Background="{StaticResource BgBrush}" Visibility="Collapsed"/>
            </Grid>
        </Border>

        <Border Grid.Column="2" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrushColor}" BorderThickness="1,0,0,0" Padding="14"
                Visibility="{Binding IsTodaySelected, Converter={StaticResource BooleanToVisibilityConverter}}">
            <views:TaskDetailPanel DataContext="{Binding TodayViewModel}" />
        </Border>
    </Grid>
</Window>
